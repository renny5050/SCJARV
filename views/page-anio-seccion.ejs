<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Años de la Sección</title>
    <link href="<%= '../css/bootstrap.min.css' %>" rel="stylesheet">
    <link rel="stylesheet" href="<%= '/css/layoutStyles.css' %>">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        :root {
            --primary: #3498db;
            --secondary: #2c3e50;
            --success: #28a745;
            --warning: #ffc107;
            --light: #f8f9fa;
            --info: #17a2b8;
            --danger: #e74c3c;
        }
        
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #f8f9fa, #e6f7ff);
            padding: 20px;
            min-height: 100vh;
        }
        
        .header {
            background: linear-gradient(135deg, var(--primary), var(--secondary));
            color: white;
            padding: 25px 0;
            margin-bottom: 30px;
            border-radius: 15px;
            box-shadow: 0 10px 20px rgba(0,0,0,0.1);
            position: relative;
            overflow: hidden;
        }
        
        .header::before {
            content: "";
            position: absolute;
            top: -50px;
            right: -50px;
            width: 200px;
            height: 200px;
            background: rgba(255, 255, 255, 0.1);
            border-radius: 50%;
        }
        
        .card {
            border-radius: 15px;
            box-shadow: 0 8px 16px rgba(0,0,0,0.08);
            margin-bottom: 25px;
            border: none;
            overflow: hidden;
            background: white;
        }
        
        .card-header {
            background: linear-gradient(to right, var(--primary), var(--secondary));
            color: white;
            font-weight: 600;
            padding: 15px 20px;
        }
        
        .btn-primary {
            background: var(--primary);
            border: none;
        }
        
        .btn-success {
            background: var(--success);
            border: none;
        }
        
        .btn-info {
            background: var(--info);
            border: none;
        }
        
        .btn-warning {
            background: var(--warning);
            border: none;
            color: #212529;
        }
        
        .btn-danger {
            background: var(--danger);
            border: none;
        }
        
        .back-button {
            display: inline-flex;
            align-items: center;
            color: var(--primary);
            text-decoration: none;
            font-weight: 500;
            transition: all 0.3s;
            margin-bottom: 20px;
            padding: 8px 15px;
            border-radius: 50px;
            background: rgba(52, 152, 219, 0.1);
        }
        
        .back-button:hover {
            color: var(--secondary);
            transform: translateX(-3px);
            background: rgba(52, 152, 219, 0.2);
        }
        
        .badge-seccion {
            background: var(--primary);
            color: white;
            padding: 5px 15px;
            border-radius: 20px;
            font-size: 1.1rem;
            font-weight: 500;
        }
        
        .empty-state {
            text-align: center;
            padding: 40px 20px;
            color: #6c757d;
        }
        
        .empty-state i {
            font-size: 4rem;
            margin-bottom: 20px;
            color: #e0e0e0;
        }
        
        .ano-table {
            width: 100%;
            border-collapse: separate;
            border-spacing: 0;
        }
        
        .ano-table thead {
            background: linear-gradient(to right, var(--info), var(--primary));
            color: white;
        }
        
        .ano-table th {
            padding: 15px;
            font-weight: 500;
            text-align: center;
        }
        
        .ano-table td {
            padding: 15px;
            border-bottom: 1px solid #e0e0e0;
            text-align: center;
            vertical-align: middle;
        }
        
        .ano-table tr:last-child td {
            border-bottom: none;
        }
        
        .ano-table tr:hover td {
            background-color: rgba(52, 152, 219, 0.05);
        }
        
        .btn-action {
            display: inline-flex;
            align-items: center;
            justify-content: center;
            width: 40px;
            height: 40px;
            border-radius: 10px;
            margin: 0 3px;
            transition: all 0.3s;
        }
        
        .btn-action:hover {
            transform: translateY(-3px);
            box-shadow: 0 4px 8px rgba(0,0,0,0.1);
        }
        
        .btn-view {
            background: linear-gradient(135deg, var(--primary), #2980b9);
            color: white;
        }
        
        .btn-view:hover {
            background: linear-gradient(135deg, #2980b9, var(--primary));
        }
        
        .btn-delete {
            background: linear-gradient(135deg, var(--danger), #c0392b);
            color: white;
        }
        
        .btn-delete:hover {
            background: linear-gradient(135deg, #c0392b, var(--danger));
        }
        
        .ano-highlight {
            font-size: 1.2rem;
            font-weight: 600;
            color: var(--primary);
        }
        
        .form-agregar-ano {
            display: flex;
            gap: 10px;
            padding: 20px;
            background: #f8f9fa;
            border-top: 1px solid #eee;
        }
        
        .input-ano {
            flex: 1;
            border-radius: 50px;
            padding: 10px 20px;
            border: 2px solid #e0e0e0;
            text-align: center;
            font-size: 1.1rem;
            font-weight: 500;
        }
        
        .input-ano:focus {
            border-color: var(--primary);
            box-shadow: 0 0 0 0.25rem rgba(52, 152, 219, 0.25);
        }
        
        .btn-agregar {
            border-radius: 50px;
            padding: 10px 25px;
            font-weight: 600;
        }
        
        .seccion-card {
            background: linear-gradient(135deg, #ffffff, #f1f8ff);
            border-left: 5px solid var(--primary);
        }
        
        @media (max-width: 768px) {
            .form-agregar-ano {
                flex-direction: column;
            }
            
            .ano-table {
                font-size: 0.9rem;
            }
        }
    </style>
</head>
<body>

        <!-- Sidebar -->
    <%- include('partials/partial-sidebar') %>
    
    <!-- Header -->
    <%- include('partials/partial-header') %>

    <main id="content">
    <div class="container">
        <!-- Encabezado -->
        <div class="header text-center">
            <h1><i class="fas fa-calendar-alt me-2"></i>Años de la Sección</h1>
            <p class="lead">Listado de años asignados</p>
        </div>
        
        <!-- Botón para volver -->
        <a href="/secciones" class="back-button">
            <i class="fas fa-arrow-left me-2"></i>Volver a secciones
        </a>
        
        <!-- Información de la sección -->
        <div class="card seccion-card mb-4">
            <div class="card-body">
                <div class="d-flex justify-content-between align-items-center">
                    <div>
                        <h3><i class="fas fa-layer-group me-2"></i>Sección: <%= seccionNombre || 'No especificada' %></h3>
                        
                    </div>
                   
                </div>
            </div>
        </div>
        
        <!-- Listado de años en tabla -->
        <div class="card">
            <div class="card-header">
                <i class="fas fa-list me-2"></i>Años Asignados
            </div>
            
            <% if (anoSeccion.length > 0) { %>
                <div class="table-responsive">
                    <table class="table ano-table">
                        <thead>
                            <tr>
                                <th>Código</th>
                                <th>Año</th>
                                <th>Acciones</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% anoSeccion.forEach(asignacion => { %>
                                <tr>
                                    <td><span class="badge bg-secondary"><%= asignacion.cod_anoSecci %></span></td>
                                    <td class="ano-highlight"><%= asignacion.ano_Seccione %></td>
                                    <td>
                                        <% if (cedulaEscolar) { %>
                                            <a href="/cursantes/<%= cod_anoSecci %>/<%= cedulaEscolar %>" 
                                                class="btn btn-success btn-action btn-view d-inline-flex align-items-center"
                                                style="font-weight:600; font-size:1rem; gap:8px; padding: 8px 18px; min-width: 120px;"
                                                title="Inscribir cursante en este año">
                                                <i class="fas fa-user-plus"></i>
                                                <span style="white-space:nowrap;">Inscribir</span>
                                            </a>
                                        <% } else {%>
                                            
                                        <!-- Botón para ir a /cursantes/<codigoseccion>/<año> -->
                                        <a href="/cursantes/<%= asignacion.cod_anoSecci %>/" 
                                           class="btn btn-action btn-view"
                                           title="Ver detalles del año">
                                            <i class="fas fa-eye"></i>
                                        </a>
                                        
                                        <!-- Botón para eliminar año -->
                                        <form action="/seccion/<%= asignacion.cod_anoSecci %>/eliminar" method="POST" class="d-inline">
                                            <input type="hidden" name="_method" value="DELETE">
                                            <button type="submit" 
                                                    class="btn btn-action btn-delete"
                                                    title="Eliminar año"
                                                    onclick="return confirm('¿Eliminar el año <%= asignacion.ano_Seccione %>?');">
                                                <i class="fas fa-trash-alt"></i>
                                            </button>
                                        </form>
                                        <% } %>
                                    </td>
                                </tr>
                            <% }); %>
                        </tbody>
                    </table>
                </div>
            <% } else { %>
                <div class="empty-state">
                    <i class="far fa-calendar-alt"></i>
                    <h3>No hay años asignados</h3>
                    <p class="text-muted">Agrega años usando el formulario inferior</p>
                </div>
            <% } %>
            
            <!-- Formulario para agregar año -->
            <form action="/seccion/<%= codigo_secci %>" method="POST" class="form-agregar-ano">
                <input type="hidden" name="codigo_secci" value="<%= codigo_secci %>">
                <input type="number" name="ano_Seccione" 
                       class="form-control input-ano" 
                       min="2000" max="2100" 
                       placeholder="Ingresa un año (ej: 2023)" 
                       required>
                
                <select name="docente_prin" id="docente_prin" class="docente-select" required>
                    <option value="">Seleccione un docente</option>
                    <% docentes.forEach(docente => { %>
                        <option value="<%= docente.codigo_perso %>">
                            <%= docente.primer_nombr %> <%= docente.primer_apell %>
                        </option>
                    <% }); %>
                </select>

                <select name="docente_secu" id="docente_secu" class="docente-select" required>
                    <option value="">Seleccione un docente</option>
                    <% docentes.forEach(docente => { %>
                        <option value="<%= docente.codigo_perso %>">
                            <%= docente.primer_nombr %> <%= docente.primer_apell %>
                        </option>
                    <% }); %>
                </select>
                <button type="submit" class="btn btn-success btn-agregar">
                    <i class="fas fa-plus me-2"></i>Agregar Año
                </button>
            </form>
        </div>
        

        <div class="text-center mt-5 text-muted">
            <p>Sistema de Gestión de Secciones &copy; <%= new Date().getFullYear() %></p>
        </div>
    </div>
    </main>
    
<script src="<%= '../js/bootstrap.bundle.min.js' %>"></script></body>
</html>